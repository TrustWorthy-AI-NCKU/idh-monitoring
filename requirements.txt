# ===== Django 核心 =====
Django==5.2.12                  # Web 框架
asgiref==3.11.1                 # Django 的非同步支援
sqlparse==0.5.5                 # Django SQL 解析
tzdata==2025.3                  # 時區資料（Windows 必需）

# ===== Dash + Plotly（互動式圖表）=====
dash==2.18.2                    # Plotly Dash（interpret 需要 <3.0.0，已降版）
dash-core-components==2.0.0     # Dash 核心元件
dash-html-components==2.0.0     # Dash HTML 元件
dash-table==5.0.0               # Dash 表格元件
dash_cytoscape==1.0.2           # Dash 網路圖元件
plotly==6.6.0                   # Plotly 圖表庫（折線圖、熱力圖）
django-plotly-dash==2.5.0       # 將 Dash 嵌入 Django 的整合套件
dpd_components==0.2.0           # django-plotly-dash 的 Dash 元件
dash-bootstrap-components==2.0.4  # Dash Bootstrap 樣式元件

# ===== Django 擴充 =====
python-decouple==3.8            # 從 .env 讀取設定（SECRET_KEY 等機密）
django-bootstrap5==26.2         # django-plotly-dash 需要的 Bootstrap 支援
channels==4.3.2                 # Django Channels（WebSocket，Dash 需要）

# ===== EBM 模型（interpret）=====
interpret==0.7.6                # EBM 模型主套件（ExplainableBoostingClassifier）
interpret-core==0.7.6           # interpret 核心函式庫
aplr==10.22.0                   # interpret 依賴：Additive PLR
shap==0.51.0                    # interpret 依賴：SHAP 特徵解釋
SALib==1.5.2                    # interpret 依賴：敏感度分析
gevent==25.9.1                  # interpret 依賴：非同步 I/O
greenlet==3.3.2                 # gevent 依賴
cffi==2.0.0                     # C 擴充介面（gevent 需要）
pycparser==3.0                  # cffi 依賴
zope.interface==8.2             # gevent 依賴
zope.event==6.1                 # gevent 依賴

# ===== 資料科學 / ML（模型監控核心）=====
pandas==3.0.1                   # 資料處理（DataFrame）
numpy==2.4.3                    # 數值計算
scikit-learn==1.8.0             # ML 指標計算（AUROC, AUPRC, F1）
scipy==1.17.1                   # JS Divergence 計算
joblib==1.5.3                   # 載入 .joblib 模型檔

# ===== 視覺化（interpret/shap 需要）=====
matplotlib==3.10.8              # interpret 依賺：繪圖
pillow==12.1.1                  # matplotlib 圖片支援
contourpy==1.3.3                # matplotlib 依賴
cycler==0.12.1                  # matplotlib 依賴
fonttools==4.61.1               # matplotlib 依賴
kiwisolver==1.4.9               # matplotlib 依賴
pyparsing==3.3.2                # matplotlib 依賴

# ===== Jupyter/IPython（interpret notebook 支援）=====
ipykernel==7.2.0
ipython==9.10.0
ipython_pygments_lexers==1.1.1
jupyter_client==8.8.0
jupyter_core==5.9.1
debugpy==1.8.20
pyzmq==27.1.0
tornado==6.5.4
traitlets==5.14.3
comm==0.2.3
psutil==7.2.2
matplotlib-inline==0.2.1
platformdirs==4.9.4
jedi==0.19.2
parso==0.8.6
pygments==2.19.2
prompt_toolkit==3.0.52
wcwidth==0.6.0
stack-data==0.6.3
asttokens==3.0.1
executing==2.2.1
pure_eval==0.2.3
decorator==5.2.1

# ===== shap/numba 依賴 =====
numba==0.64.0                   # shap 加速計算
llvmlite==0.46.0                # numba 依賴
cloudpickle==3.1.2              # shap 依賴
tqdm==4.67.3                    # 進度條
slicer==0.0.8                   # shap 依賴
multiprocess==0.70.19           # SALib/shap 平行計算
dill==0.4.1                     # multiprocess 依賴

# ===== Dash/Flask 底層依賴 =====
Flask==3.0.3                    # Dash 底層 Web 框架
Werkzeug==3.0.6                 # Flask 的 HTTP 工具
Jinja2==3.1.6                   # Flask 模板引擎
MarkupSafe==3.0.3               # Jinja2 安全轉義
click==8.3.1                    # Flask CLI 工具
blinker==1.9.0                  # Flask 信號系統
itsdangerous==2.2.0             # Flask session 安全簽名
nest-asyncio==1.6.0             # Dash 非同步支援

# ===== 網路/序列化相關 =====
requests==2.32.5                # HTTP 請求
certifi==2026.2.25              # SSL 憑證
charset-normalizer==3.4.5       # 字元編碼
idna==3.11                      # 國際化域名
urllib3==2.6.3                   # HTTP 低層套件

# ===== 其他工具 =====
narwhals==2.17.0                # Plotly 的 DataFrame 相容層
packaging==26.0                 # 版本解析
python-dateutil==2.9.0.post0    # 日期處理
six==1.17.0                     # Python 2/3 相容
retrying==1.4.2                 # 重試機制
threadpoolctl==3.6.0            # scikit-learn 執行緒控制
typing_extensions==4.15.0       # 型別提示擴充
importlib_metadata==8.7.1       # 套件元資料
zipp==3.23.0                    # ZIP 檔案處理
colorama==0.4.6                 # Windows 終端上色

# ===== LLM（語言模型分析）=====
ollama                          # Ollama Python 用戶端（連線遠端 LLM）
